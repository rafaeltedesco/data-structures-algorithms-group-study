[project]
name = "03-symmetric-difference"
version = "0.1.0"
description = "A Python project to compute the symmetric difference between two lists and sort sets of integers."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.5",
]
